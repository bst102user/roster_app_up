import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class CommonWidgets{
  Widget MyButton(
      var funName,

      )
  {
    return MaterialButton(onPressed: funName);
  }
}